Gulp-Require-jQuery-Playground
==============================

To get started, execute the following commands in you terminal:

    npm install -g bower gulp
    npm install
    bower install
    gulp
